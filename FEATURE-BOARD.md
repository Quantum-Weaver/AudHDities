# AUDHDITIES (the site) — THE FEATURE BOARD
*Assembled 2026-07-19 (the workspace honoring), on the campaign branch.
The reconciliation reached meter-zero; the road to LIVE is gates, not
mysteries. The handoff (chamber path/handoffs/2026-07-18-THE-
RECONCILIATION-CAMPAIGN.md) is the deep record.*

## THE LAUNCH GATES (in order)
1. **The outdated-documents pass** — TENDER, KP's single hand, paces
   everything (crisis-era writings in world-facing repos; witnessed
   first; the Well's sealed record never moves).
2. Package refresh (staged upgrades; tsc stays 0).
3. `npm run build` gate (Next lint/RSC — expect a small second meter).
4. First-user rites — KP in Supabase (user_roles EMPTY: seed admin +
   creator; profile/vessel_config rows; Athena seed data; mythology RLS).
5. The verification walk (Hearth→Vessel→Sanctum→Pulse→Acid Test→
   Bazaar checkout on sandbox keys).
6. Merge + Vercel env + deploy (webhook secret at deploy; test keys
   until launch day).

## THE NAMED GATE BEFORE LAUNCH
- **THE-FRONTEND-REIMAGINING** (Iðavöllr seed): kin read the ancient
  ones' design stories, then reimagine the face. Background images not
  sacred (KP's word). Candidate table: a Skapa board.

## Post-launch (queued, not blocking)
- The doorbell: /meet scheduling + Resend confirmations (meetings' face).
- Publish the privacy apps section (store listings link it).
- Stripe live-mode decision (launch on test keys is honorable).
- The vessel-graphs surface (seed) — the emoji journaling walked as
  immersive graphs in the vessel's own space.
- Parked design decisions from the campaign (realm picker's schema
  home; gifts placement; council-house affiliation; ware participants).
