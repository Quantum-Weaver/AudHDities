# MIRROR — database.types.ts

The single editable truth is `schema/superposition/database.types.ts`,
delivered by the types courier (`npm run types`). This copy exists so
`./database.types` is the same call in the yard as it is everywhere
the layer is distributed — never edit it here; rerun the foundation.

sha256 (source, full): 4459d24cf0e883d26a2d3cfa59a88b948d2895117c990d59ce1767fdbc29a9fb
