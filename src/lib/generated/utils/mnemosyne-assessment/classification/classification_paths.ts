// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: classification_paths · deity: classification · handling: identity
// schema_hash: fb9fcf787a4e071eda3d02c2a59fffb2 · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { ClassificationPathsInsertSchema, ClassificationPathsUpdateSchema } from '../../../validators/mnemosyne-assessment/classification/classification_paths';
import type { ClassificationPathsInsert, ClassificationPathsRow, ClassificationPathsUpdate } from '../../../types/mnemosyne-assessment/classification/classification_paths';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new classification_paths record
 */
export async function createClassificationPaths(data: ClassificationPathsInsert): Promise<ClassificationPathsRow> {
  const validated = ClassificationPathsInsertSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('classification_paths')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single classification_paths record by ID
 */
export async function getClassificationPaths(id: string): Promise<ClassificationPathsRow> {
  const supabase = createClient('knowledge');
  const { data, error } = await supabase
    .from('classification_paths')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of classification_paths records with pagination
 */
export async function listClassificationPaths(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: ClassificationPathsRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient('knowledge');

  let query = supabase.from('classification_paths').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a classification_paths record
 */
export async function updateClassificationPaths(id: string, data: ClassificationPathsUpdate): Promise<ClassificationPathsRow> {
  const validated = ClassificationPathsUpdateSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('classification_paths')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a classification_paths record
 */
export async function deleteClassificationPaths(id: string): Promise<boolean> {
  const supabase = createClient('knowledge');
  const { error } = await supabase
    .from('classification_paths')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
