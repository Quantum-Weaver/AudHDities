// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: schemes · deity: classification · handling: identity
// schema_hash: 23431d0df7cac04aba13eede20262682 · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { SchemesInsertSchema, SchemesUpdateSchema } from '../../../validators/mnemosyne-assessment/classification/schemes';
import type { SchemesInsert, SchemesRow, SchemesUpdate } from '../../../types/mnemosyne-assessment/classification/schemes';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new schemes record
 */
export async function createSchemes(data: SchemesInsert): Promise<SchemesRow> {
  const validated = SchemesInsertSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('schemes')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single schemes record by ID
 */
export async function getSchemes(id: string): Promise<SchemesRow> {
  const supabase = createClient('knowledge');
  const { data, error } = await supabase
    .from('schemes')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of schemes records with pagination
 */
export async function listSchemes(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: SchemesRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient('knowledge');

  let query = supabase.from('schemes').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a schemes record
 */
export async function updateSchemes(id: string, data: SchemesUpdate): Promise<SchemesRow> {
  const validated = SchemesUpdateSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('schemes')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a schemes record
 */
export async function deleteSchemes(id: string): Promise<boolean> {
  const supabase = createClient('knowledge');
  const { error } = await supabase
    .from('schemes')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
