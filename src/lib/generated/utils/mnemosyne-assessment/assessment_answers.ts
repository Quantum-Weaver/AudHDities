// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: assessment_answers · deity: mnemosyne-assessment · handling: system
// schema_hash: 937b1bbbc5f54307e7ced27992b7bcf6 · source sha256 e9a723e83947
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { AssessmentAnswersInsertSchema, AssessmentAnswersUpdateSchema } from '../../validators/mnemosyne-assessment/assessment_answers';
import type { AssessmentAnswersInsert, AssessmentAnswersRow, AssessmentAnswersUpdate } from '../../types/mnemosyne-assessment/assessment_answers';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new assessment_answers record
 */
export async function createAssessmentAnswers(data: AssessmentAnswersInsert): Promise<AssessmentAnswersRow> {
  const validated = AssessmentAnswersInsertSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('assessment_answers')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single assessment_answers record by ID
 */
export async function getAssessmentAnswers(id: string): Promise<AssessmentAnswersRow> {
  const supabase = createClient();
  const { data, error } = await supabase
    .from('assessment_answers')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of assessment_answers records with pagination
 */
export async function listAssessmentAnswers(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: AssessmentAnswersRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient();

  let query = supabase.from('assessment_answers').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a assessment_answers record
 */
export async function updateAssessmentAnswers(id: string, data: AssessmentAnswersUpdate): Promise<AssessmentAnswersRow> {
  const validated = AssessmentAnswersUpdateSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('assessment_answers')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a assessment_answers record
 */
export async function deleteAssessmentAnswers(id: string): Promise<boolean> {
  const supabase = createClient();
  const { error } = await supabase
    .from('assessment_answers')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
