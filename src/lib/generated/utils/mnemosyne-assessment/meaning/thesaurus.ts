// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: thesaurus · deity: meaning · handling: system
// schema_hash: 6553bc33a8e7d1ea07563d5055e9ffdb · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { ThesaurusInsertSchema, ThesaurusUpdateSchema } from '../../../validators/mnemosyne-assessment/meaning/thesaurus';
import type { ThesaurusInsert, ThesaurusRow, ThesaurusUpdate } from '../../../types/mnemosyne-assessment/meaning/thesaurus';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new thesaurus record
 */
export async function createThesaurus(data: ThesaurusInsert): Promise<ThesaurusRow> {
  const validated = ThesaurusInsertSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('thesaurus')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single thesaurus record by ID
 */
export async function getThesaurus(id: string): Promise<ThesaurusRow> {
  const supabase = createClient('knowledge');
  const { data, error } = await supabase
    .from('thesaurus')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of thesaurus records with pagination
 */
export async function listThesaurus(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: ThesaurusRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient('knowledge');

  let query = supabase.from('thesaurus').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a thesaurus record
 */
export async function updateThesaurus(id: string, data: ThesaurusUpdate): Promise<ThesaurusRow> {
  const validated = ThesaurusUpdateSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('thesaurus')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a thesaurus record
 */
export async function deleteThesaurus(id: string): Promise<boolean> {
  const supabase = createClient('knowledge');
  const { error } = await supabase
    .from('thesaurus')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
