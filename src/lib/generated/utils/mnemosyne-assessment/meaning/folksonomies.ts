// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: folksonomies · deity: meaning · handling: system
// schema_hash: d7e48272d9a7641d3bd0181f88e1eddf · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { FolksonomiesInsertSchema, FolksonomiesUpdateSchema } from '../../../validators/mnemosyne-assessment/meaning/folksonomies';
import type { FolksonomiesInsert, FolksonomiesRow, FolksonomiesUpdate } from '../../../types/mnemosyne-assessment/meaning/folksonomies';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new folksonomies record
 */
export async function createFolksonomies(data: FolksonomiesInsert): Promise<FolksonomiesRow> {
  const validated = FolksonomiesInsertSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('folksonomies')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single folksonomies record by ID
 */
export async function getFolksonomies(id: string): Promise<FolksonomiesRow> {
  const supabase = createClient('knowledge');
  const { data, error } = await supabase
    .from('folksonomies')
    .select('*')
    .eq('name', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of folksonomies records with pagination
 */
export async function listFolksonomies(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: FolksonomiesRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient('knowledge');

  let query = supabase.from('folksonomies').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a folksonomies record
 */
export async function updateFolksonomies(id: string, data: FolksonomiesUpdate): Promise<FolksonomiesRow> {
  const validated = FolksonomiesUpdateSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('folksonomies')
    .update(validated)
    .eq('name', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a folksonomies record
 */
export async function deleteFolksonomies(id: string): Promise<boolean> {
  const supabase = createClient('knowledge');
  const { error } = await supabase
    .from('folksonomies')
    .delete()
    .eq('name', id);

  if (error) throw error;
  return true;
}
