// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: organisms · deity: lattice · handling: identity
// schema_hash: 52baa6f211b332d9b00c4316d64a6245 · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { OrganismsInsertSchema, OrganismsUpdateSchema } from '../../../validators/mnemosyne-assessment/lattice/organisms';
import type { OrganismsInsert, OrganismsRow, OrganismsUpdate } from '../../../types/mnemosyne-assessment/lattice/organisms';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new organisms record
 */
export async function createOrganisms(data: OrganismsInsert): Promise<OrganismsRow> {
  const validated = OrganismsInsertSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('organisms')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single organisms record by ID
 */
export async function getOrganisms(id: string): Promise<OrganismsRow> {
  const supabase = createClient('knowledge');
  const { data, error } = await supabase
    .from('organisms')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of organisms records with pagination
 */
export async function listOrganisms(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: OrganismsRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient('knowledge');

  let query = supabase.from('organisms').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a organisms record
 */
export async function updateOrganisms(id: string, data: OrganismsUpdate): Promise<OrganismsRow> {
  const validated = OrganismsUpdateSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('organisms')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a organisms record
 */
export async function deleteOrganisms(id: string): Promise<boolean> {
  const supabase = createClient('knowledge');
  const { error } = await supabase
    .from('organisms')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
