// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: molecules · deity: lattice · handling: identity
// schema_hash: acee64e0d5d7c0ad766338d99def3add · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { MoleculesInsertSchema, MoleculesUpdateSchema } from '../../../validators/mnemosyne-assessment/lattice/molecules';
import type { MoleculesInsert, MoleculesRow, MoleculesUpdate } from '../../../types/mnemosyne-assessment/lattice/molecules';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new molecules record
 */
export async function createMolecules(data: MoleculesInsert): Promise<MoleculesRow> {
  const validated = MoleculesInsertSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('molecules')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single molecules record by ID
 */
export async function getMolecules(id: string): Promise<MoleculesRow> {
  const supabase = createClient('knowledge');
  const { data, error } = await supabase
    .from('molecules')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of molecules records with pagination
 */
export async function listMolecules(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: MoleculesRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient('knowledge');

  let query = supabase.from('molecules').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a molecules record
 */
export async function updateMolecules(id: string, data: MoleculesUpdate): Promise<MoleculesRow> {
  const validated = MoleculesUpdateSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('molecules')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a molecules record
 */
export async function deleteMolecules(id: string): Promise<boolean> {
  const supabase = createClient('knowledge');
  const { error } = await supabase
    .from('molecules')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
