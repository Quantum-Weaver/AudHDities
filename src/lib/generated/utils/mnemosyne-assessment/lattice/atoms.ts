// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: atoms · deity: lattice · handling: identity
// schema_hash: a137f60d823cd6ce6fd83cf366fd1833 · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { AtomsInsertSchema, AtomsUpdateSchema } from '../../../validators/mnemosyne-assessment/lattice/atoms';
import type { AtomsInsert, AtomsRow, AtomsUpdate } from '../../../types/mnemosyne-assessment/lattice/atoms';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new atoms record
 */
export async function createAtoms(data: AtomsInsert): Promise<AtomsRow> {
  const validated = AtomsInsertSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('atoms')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single atoms record by ID
 */
export async function getAtoms(id: string): Promise<AtomsRow> {
  const supabase = createClient('knowledge');
  const { data, error } = await supabase
    .from('atoms')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of atoms records with pagination
 */
export async function listAtoms(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: AtomsRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient('knowledge');

  let query = supabase.from('atoms').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a atoms record
 */
export async function updateAtoms(id: string, data: AtomsUpdate): Promise<AtomsRow> {
  const validated = AtomsUpdateSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('atoms')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a atoms record
 */
export async function deleteAtoms(id: string): Promise<boolean> {
  const supabase = createClient('knowledge');
  const { error } = await supabase
    .from('atoms')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
