// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: maintenance · deity: hephaestus-infrastructure · handling: system
// schema_hash: 0ec76bc91cfe5c80da10f940bcca3bbb · source sha256 983e24fa2371
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { MaintenanceInsertSchema, MaintenanceUpdateSchema } from '../../validators/hephaestus-infrastructure/maintenance';
import type { MaintenanceInsert, MaintenanceRow, MaintenanceUpdate } from '../../types/hephaestus-infrastructure/maintenance';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new maintenance record
 */
export async function createMaintenance(data: MaintenanceInsert): Promise<MaintenanceRow> {
  const validated = MaintenanceInsertSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('maintenance')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single maintenance record by ID
 */
export async function getMaintenance(id: string): Promise<MaintenanceRow> {
  const supabase = createClient();
  const { data, error } = await supabase
    .from('maintenance')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of maintenance records with pagination
 */
export async function listMaintenance(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: MaintenanceRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient();

  let query = supabase.from('maintenance').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a maintenance record
 */
export async function updateMaintenance(id: string, data: MaintenanceUpdate): Promise<MaintenanceRow> {
  const validated = MaintenanceUpdateSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('maintenance')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a maintenance record
 */
export async function deleteMaintenance(id: string): Promise<boolean> {
  const supabase = createClient();
  const { error } = await supabase
    .from('maintenance')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
