// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: bookings · deity: hephaestus-infrastructure · handling: full_crud
// schema_hash: d05d03b826a0ea6d25b474e2c90a9bc6 · source sha256 130478f299af
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { BookingsInsertSchema, BookingsUpdateSchema } from '../../validators/hephaestus-infrastructure/bookings';
import type { BookingsInsert, BookingsRow, BookingsUpdate } from '../../types/hephaestus-infrastructure/bookings';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new bookings record
 */
export async function createBookings(data: BookingsInsert): Promise<BookingsRow> {
  const validated = BookingsInsertSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('bookings')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single bookings record by ID
 */
export async function getBookings(id: string): Promise<BookingsRow> {
  const supabase = createClient();
  const { data, error } = await supabase
    .from('bookings')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of bookings records with pagination
 */
export async function listBookings(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: BookingsRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient();

  let query = supabase.from('bookings').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a bookings record
 */
export async function updateBookings(id: string, data: BookingsUpdate): Promise<BookingsRow> {
  const validated = BookingsUpdateSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('bookings')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a bookings record
 */
export async function deleteBookings(id: string): Promise<boolean> {
  const supabase = createClient();
  const { error } = await supabase
    .from('bookings')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
