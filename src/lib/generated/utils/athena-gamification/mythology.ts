// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: mythology · deity: athena-gamification · handling: system
// schema_hash: b027378461d6734cd1672338225133ac · source sha256 983e24fa2371
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { MythologyInsertSchema, MythologyUpdateSchema } from '../../validators/athena-gamification/mythology';
import type { MythologyInsert, MythologyRow, MythologyUpdate } from '../../types/athena-gamification/mythology';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new mythology record
 */
export async function createMythology(data: MythologyInsert): Promise<MythologyRow> {
  const validated = MythologyInsertSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('mythology')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single mythology record by ID
 */
export async function getMythology(id: string): Promise<MythologyRow> {
  const supabase = createClient();
  const { data, error } = await supabase
    .from('mythology')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of mythology records with pagination
 */
export async function listMythology(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: MythologyRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient();

  let query = supabase.from('mythology').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a mythology record
 */
export async function updateMythology(id: string, data: MythologyUpdate): Promise<MythologyRow> {
  const validated = MythologyUpdateSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('mythology')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a mythology record
 */
export async function deleteMythology(id: string): Promise<boolean> {
  const supabase = createClient();
  const { error } = await supabase
    .from('mythology')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
