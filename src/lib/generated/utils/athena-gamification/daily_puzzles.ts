// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: daily_puzzles · deity: athena-gamification · handling: full_crud
// schema_hash: 0f0f4a9a8aec5537e3f8b0acb618166a · source sha256 7bc83d285cfd
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { DailyPuzzlesInsertSchema, DailyPuzzlesUpdateSchema } from '../../validators/athena-gamification/daily_puzzles';
import type { DailyPuzzlesInsert, DailyPuzzlesRow, DailyPuzzlesUpdate } from '../../types/athena-gamification/daily_puzzles';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new daily_puzzles record
 */
export async function createDailyPuzzles(data: DailyPuzzlesInsert): Promise<DailyPuzzlesRow> {
  const validated = DailyPuzzlesInsertSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('daily_puzzles')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single daily_puzzles record by ID
 */
export async function getDailyPuzzles(id: string): Promise<DailyPuzzlesRow> {
  const supabase = createClient();
  const { data, error } = await supabase
    .from('daily_puzzles')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of daily_puzzles records with pagination
 */
export async function listDailyPuzzles(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: DailyPuzzlesRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient();

  let query = supabase.from('daily_puzzles').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a daily_puzzles record
 */
export async function updateDailyPuzzles(id: string, data: DailyPuzzlesUpdate): Promise<DailyPuzzlesRow> {
  const validated = DailyPuzzlesUpdateSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('daily_puzzles')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a daily_puzzles record
 */
export async function deleteDailyPuzzles(id: string): Promise<boolean> {
  const supabase = createClient();
  const { error } = await supabase
    .from('daily_puzzles')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
