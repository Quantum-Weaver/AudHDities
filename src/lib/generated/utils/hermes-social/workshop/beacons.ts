// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: beacons · deity: workshop · handling: system
// schema_hash: f7e86e070ea5a1544714c61c7423dafc · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { BeaconsInsertSchema, BeaconsUpdateSchema } from '../../../validators/hermes-social/workshop/beacons';
import type { BeaconsInsert, BeaconsRow, BeaconsUpdate } from '../../../types/hermes-social/workshop/beacons';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new beacons record
 */
export async function createBeacons(data: BeaconsInsert): Promise<BeaconsRow> {
  const validated = BeaconsInsertSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('beacons')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single beacons record by ID
 */
export async function getBeacons(id: string): Promise<BeaconsRow> {
  const supabase = createClient('knowledge');
  const { data, error } = await supabase
    .from('beacons')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of beacons records with pagination
 */
export async function listBeacons(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: BeaconsRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient('knowledge');

  let query = supabase.from('beacons').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a beacons record
 */
export async function updateBeacons(id: string, data: BeaconsUpdate): Promise<BeaconsRow> {
  const validated = BeaconsUpdateSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('beacons')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a beacons record
 */
export async function deleteBeacons(id: string): Promise<boolean> {
  const supabase = createClient('knowledge');
  const { error } = await supabase
    .from('beacons')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
