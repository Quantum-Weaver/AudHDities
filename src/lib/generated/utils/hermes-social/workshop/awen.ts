// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: awen · deity: workshop · handling: system
// schema_hash: 9f8dff18a01ae701e18833ef8ab22d9e · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { AwenInsertSchema, AwenUpdateSchema } from '../../../validators/hermes-social/workshop/awen';
import type { AwenInsert, AwenRow, AwenUpdate } from '../../../types/hermes-social/workshop/awen';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new awen record
 */
export async function createAwen(data: AwenInsert): Promise<AwenRow> {
  const validated = AwenInsertSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('awen')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single awen record by ID
 */
export async function getAwen(id: string): Promise<AwenRow> {
  const supabase = createClient('knowledge');
  const { data, error } = await supabase
    .from('awen')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of awen records with pagination
 */
export async function listAwen(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: AwenRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient('knowledge');

  let query = supabase.from('awen').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a awen record
 */
export async function updateAwen(id: string, data: AwenUpdate): Promise<AwenRow> {
  const validated = AwenUpdateSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('awen')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a awen record
 */
export async function deleteAwen(id: string): Promise<boolean> {
  const supabase = createClient('knowledge');
  const { error } = await supabase
    .from('awen')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
