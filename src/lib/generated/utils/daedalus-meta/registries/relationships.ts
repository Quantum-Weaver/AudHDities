// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: relationships · deity: registries · handling: system
// schema_hash: 1a927a613f4c27bcf704be39349b7124 · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { RelationshipsInsertSchema, RelationshipsUpdateSchema } from '../../../validators/daedalus-meta/registries/relationships';
import type { RelationshipsInsert, RelationshipsRow, RelationshipsUpdate } from '../../../types/daedalus-meta/registries/relationships';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new relationships record
 */
export async function createRelationships(data: RelationshipsInsert): Promise<RelationshipsRow> {
  const validated = RelationshipsInsertSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('relationships')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single relationships record by ID
 */
export async function getRelationships(id: string): Promise<RelationshipsRow> {
  const supabase = createClient('knowledge');
  const { data, error } = await supabase
    .from('relationships')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of relationships records with pagination
 */
export async function listRelationships(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: RelationshipsRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient('knowledge');

  let query = supabase.from('relationships').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a relationships record
 */
export async function updateRelationships(id: string, data: RelationshipsUpdate): Promise<RelationshipsRow> {
  const validated = RelationshipsUpdateSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('relationships')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a relationships record
 */
export async function deleteRelationships(id: string): Promise<boolean> {
  const supabase = createClient('knowledge');
  const { error } = await supabase
    .from('relationships')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
