// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: policies · deity: registries · handling: system
// schema_hash: d6a101cbd24de86618ccae5a0e0900d0 · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { PoliciesInsertSchema, PoliciesUpdateSchema } from '../../../validators/daedalus-meta/registries/policies';
import type { PoliciesInsert, PoliciesRow, PoliciesUpdate } from '../../../types/daedalus-meta/registries/policies';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new policies record
 */
export async function createPolicies(data: PoliciesInsert): Promise<PoliciesRow> {
  const validated = PoliciesInsertSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('policies')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single policies record by ID
 */
export async function getPolicies(id: string): Promise<PoliciesRow> {
  const supabase = createClient('knowledge');
  const { data, error } = await supabase
    .from('policies')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of policies records with pagination
 */
export async function listPolicies(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: PoliciesRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient('knowledge');

  let query = supabase.from('policies').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a policies record
 */
export async function updatePolicies(id: string, data: PoliciesUpdate): Promise<PoliciesRow> {
  const validated = PoliciesUpdateSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('policies')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a policies record
 */
export async function deletePolicies(id: string): Promise<boolean> {
  const supabase = createClient('knowledge');
  const { error } = await supabase
    .from('policies')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
