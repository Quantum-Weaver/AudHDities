// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: indexes · deity: registries · handling: system
// schema_hash: 1bd4eb4ebc6c1980897e58b178e44fe8 · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { IndexesInsertSchema, IndexesUpdateSchema } from '../../../validators/daedalus-meta/registries/indexes';
import type { IndexesInsert, IndexesRow, IndexesUpdate } from '../../../types/daedalus-meta/registries/indexes';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new indexes record
 */
export async function createIndexes(data: IndexesInsert): Promise<IndexesRow> {
  const validated = IndexesInsertSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('indexes')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single indexes record by ID
 */
export async function getIndexes(id: string): Promise<IndexesRow> {
  const supabase = createClient('knowledge');
  const { data, error } = await supabase
    .from('indexes')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of indexes records with pagination
 */
export async function listIndexes(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: IndexesRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient('knowledge');

  let query = supabase.from('indexes').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a indexes record
 */
export async function updateIndexes(id: string, data: IndexesUpdate): Promise<IndexesRow> {
  const validated = IndexesUpdateSchema.parse(data);
  const supabase = createClient('knowledge');

  const { data: result, error } = await supabase
    .from('indexes')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a indexes record
 */
export async function deleteIndexes(id: string): Promise<boolean> {
  const supabase = createClient('knowledge');
  const { error } = await supabase
    .from('indexes')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
