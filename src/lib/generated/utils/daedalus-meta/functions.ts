// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: functions · deity: daedalus-meta · handling: system
// schema_hash: 53a6276f2497c3a9f1ba715ee877392e · source sha256 e9a723e83947
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { FunctionsInsertSchema, FunctionsUpdateSchema } from '../../validators/daedalus-meta/functions';
import type { FunctionsInsert, FunctionsRow, FunctionsUpdate } from '../../types/daedalus-meta/functions';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new functions record
 */
export async function createFunctions(data: FunctionsInsert): Promise<FunctionsRow> {
  const validated = FunctionsInsertSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('functions')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single functions record by ID
 */
export async function getFunctions(id: string): Promise<FunctionsRow> {
  const supabase = createClient();
  const { data, error } = await supabase
    .from('functions')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of functions records with pagination
 */
export async function listFunctions(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: FunctionsRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient();

  let query = supabase.from('functions').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a functions record
 */
export async function updateFunctions(id: string, data: FunctionsUpdate): Promise<FunctionsRow> {
  const validated = FunctionsUpdateSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('functions')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a functions record
 */
export async function deleteFunctions(id: string): Promise<boolean> {
  const supabase = createClient();
  const { error } = await supabase
    .from('functions')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
