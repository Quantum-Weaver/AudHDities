// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: triggers · deity: daedalus-meta · handling: system
// schema_hash: 4e1d82a4c294c11ccdc28bbb6d5ca4b4 · source sha256 983e24fa2371
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { TriggersInsertSchema, TriggersUpdateSchema } from '../../validators/daedalus-meta/triggers';
import type { TriggersInsert, TriggersRow, TriggersUpdate } from '../../types/daedalus-meta/triggers';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new triggers record
 */
export async function createTriggers(data: TriggersInsert): Promise<TriggersRow> {
  const validated = TriggersInsertSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('triggers')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single triggers record by ID
 */
export async function getTriggers(id: string): Promise<TriggersRow> {
  const supabase = createClient();
  const { data, error } = await supabase
    .from('triggers')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of triggers records with pagination
 */
export async function listTriggers(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: TriggersRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient();

  let query = supabase.from('triggers').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a triggers record
 */
export async function updateTriggers(id: string, data: TriggersUpdate): Promise<TriggersRow> {
  const validated = TriggersUpdateSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('triggers')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a triggers record
 */
export async function deleteTriggers(id: string): Promise<boolean> {
  const supabase = createClient();
  const { error } = await supabase
    .from('triggers')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
