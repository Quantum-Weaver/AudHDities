// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: role_catalog · deity: hestia-core · handling: full_crud
// schema_hash: f1b30e8b4e1f089213d1fd6a36dbbaa2 · source sha256 4459d24cf0e8
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { RoleCatalogInsertSchema, RoleCatalogUpdateSchema } from '../../validators/hestia-core/role_catalog';
import type { RoleCatalogInsert, RoleCatalogRow, RoleCatalogUpdate } from '../../types/hestia-core/role_catalog';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new role_catalog record
 */
export async function createRoleCatalog(data: RoleCatalogInsert): Promise<RoleCatalogRow> {
  const validated = RoleCatalogInsertSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('role_catalog')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single role_catalog record by ID
 */
export async function getRoleCatalog(id: string): Promise<RoleCatalogRow> {
  const supabase = createClient();
  const { data, error } = await supabase
    .from('role_catalog')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of role_catalog records with pagination
 */
export async function listRoleCatalog(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: RoleCatalogRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient();

  let query = supabase.from('role_catalog').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a role_catalog record
 */
export async function updateRoleCatalog(id: string, data: RoleCatalogUpdate): Promise<RoleCatalogRow> {
  const validated = RoleCatalogUpdateSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('role_catalog')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a role_catalog record
 */
export async function deleteRoleCatalog(id: string): Promise<boolean> {
  const supabase = createClient();
  const { error } = await supabase
    .from('role_catalog')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
