// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: vessel_lessons · deity: hestia-core · handling: full_crud
// schema_hash: 0ca002253e3c2c8edc49bb572ef7396e · source sha256 130478f299af
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { VesselLessonsInsertSchema, VesselLessonsUpdateSchema } from '../../validators/hestia-core/vessel_lessons';
import type { VesselLessonsInsert, VesselLessonsRow, VesselLessonsUpdate } from '../../types/hestia-core/vessel_lessons';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new vessel_lessons record
 */
export async function createVesselLessons(data: VesselLessonsInsert): Promise<VesselLessonsRow> {
  const validated = VesselLessonsInsertSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('vessel_lessons')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single vessel_lessons record by ID
 */
export async function getVesselLessons(id: string): Promise<VesselLessonsRow> {
  const supabase = createClient();
  const { data, error } = await supabase
    .from('vessel_lessons')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of vessel_lessons records with pagination
 */
export async function listVesselLessons(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: VesselLessonsRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient();

  let query = supabase.from('vessel_lessons').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a vessel_lessons record
 */
export async function updateVesselLessons(id: string, data: VesselLessonsUpdate): Promise<VesselLessonsRow> {
  const validated = VesselLessonsUpdateSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('vessel_lessons')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a vessel_lessons record
 */
export async function deleteVesselLessons(id: string): Promise<boolean> {
  const supabase = createClient();
  const { error } = await supabase
    .from('vessel_lessons')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
