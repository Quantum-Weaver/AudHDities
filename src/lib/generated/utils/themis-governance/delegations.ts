// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: delegations · deity: themis-governance · handling: full_crud
// schema_hash: ccac0f2f75d99d5908e38bfd8842d378 · source sha256 130478f299af
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { DelegationsInsertSchema, DelegationsUpdateSchema } from '../../validators/themis-governance/delegations';
import type { DelegationsInsert, DelegationsRow, DelegationsUpdate } from '../../types/themis-governance/delegations';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new delegations record
 */
export async function createDelegations(data: DelegationsInsert): Promise<DelegationsRow> {
  const validated = DelegationsInsertSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('delegations')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single delegations record by ID
 */
export async function getDelegations(id: string): Promise<DelegationsRow> {
  const supabase = createClient();
  const { data, error } = await supabase
    .from('delegations')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of delegations records with pagination
 */
export async function listDelegations(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: DelegationsRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient();

  let query = supabase.from('delegations').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a delegations record
 */
export async function updateDelegations(id: string, data: DelegationsUpdate): Promise<DelegationsRow> {
  const validated = DelegationsUpdateSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('delegations')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a delegations record
 */
export async function deleteDelegations(id: string): Promise<boolean> {
  const supabase = createClient();
  const { error } = await supabase
    .from('delegations')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
