// =====================================================
// GENERATED UTILITIES - DO NOT EDIT MANUALLY
// =====================================================
// table: channels · deity: iris-communications · handling: owned
// schema_hash: bbf9e70f72020e0ec3e1032d024c01ae · source sha256 7bc83d285cfd
// Generated by resonance-gaia src/pieces/generate_utils_file.ts
// =====================================================

import { createClient } from '@/lib/supabase/client';
import { ChannelsInsertSchema, ChannelsUpdateSchema } from '../../validators/iris-communications/channels';
import type { ChannelsInsert, ChannelsRow, ChannelsUpdate } from '../../types/iris-communications/channels';

// ============================================================================
// CRUD OPERATIONS
// ============================================================================

/**
 * Create a new channels record
 */
export async function createChannels(data: ChannelsInsert): Promise<ChannelsRow> {
  const validated = ChannelsInsertSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('channels')
    .insert(validated)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Get a single channels record by ID
 */
export async function getChannels(id: string): Promise<ChannelsRow> {
  const supabase = createClient();
  const { data, error } = await supabase
    .from('channels')
    .select('*')
    .eq('id', id)
    .single();

  if (error) throw error;
  return data;
}

/**
 * Get a list of channels records with pagination
 */
export async function listChannels(params?: {
  page?: number;
  limit?: number;
  filters?: Record<string, string>;
  sort?: string;
  order?: 'asc' | 'desc';
}): Promise<{ data: ChannelsRow[]; total: number }> {
  const { page = 1, limit = 20, filters = {}, sort = 'created_at', order = 'desc' } = params || {};
  const supabase = createClient();

  let query = supabase.from('channels').select('*', { count: 'exact' });

  for (const [key, value] of Object.entries(filters)) {
    query = query.eq(key, value);
  }

  query = query.order(sort, { ascending: order === 'asc' });

  const from = (page - 1) * limit;
  const to = from + limit - 1;
  query = query.range(from, to);

  const { data, error, count } = await query;
  if (error) throw error;

  return { data: data || [], total: count || 0 };
}

/**
 * Update a channels record
 */
export async function updateChannels(id: string, data: ChannelsUpdate): Promise<ChannelsRow> {
  const validated = ChannelsUpdateSchema.parse(data);
  const supabase = createClient();

  const { data: result, error } = await supabase
    .from('channels')
    .update(validated)
    .eq('id', id)
    .select()
    .single();

  if (error) throw error;
  return result;
}

/**
 * Delete a channels record
 */
export async function deleteChannels(id: string): Promise<boolean> {
  const supabase = createClient();
  const { error } = await supabase
    .from('channels')
    .delete()
    .eq('id', id);

  if (error) throw error;
  return true;
}
