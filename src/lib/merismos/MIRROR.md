# DISTRIBUTED MIRROR - the source of truth lives in resonance-awen

The single editable truth for this water is:
C:\_superposition\resonance-awen\tools\the-merismos\src\index.ts

Do not edit index.ts in THIS folder - it is a byte-faithful mirror
(SHA256 at the copy: C5354365B7718AC4CDA26EBFE15810920E515FC46B8B760536643D5993781700),
taken 2026-09-02 alongside `src/lib/sphragis/`, the same day the water itself
was built.

WHY THIS REALM CONSUMES IT

The splits — who gets what, as columns, never as prose to re-argue. A
`Sphragis.collaborators` on a ware's licence is shaped exactly as this
water's `Merismos`: parts, roles, basis points, a yes and the moment it was
given, summing to 10000. This realm never imports the sibling water into
the sphragis mirror (neither water imports the other, by law); it is
mirrored here in full so the residual-system page can cite `combine()`'s
picture — the platform's ten, the artist's ninety, and the ninety divided
by the parts — as the collaborators' half of the residual pool. Nothing
here moves a cent; `shares` and `combine` return numbers to read, and the
real arithmetic stays server-side where the money lives.

Read alongside: `src/lib/sphragis/` (the-sphragis), whose `collaborators`
field this water's `Merismos` assigns straight into, by shape.

Record: `.journals/realm/2026-09-02-the-seal-on-every-ware.md`, RUN-LOG.md.
