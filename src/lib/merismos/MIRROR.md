# DISTRIBUTED MIRROR - the source of truth lives in resonance-awen

The single editable truth for this water is:
C:\_superposition\resonance-awen\tools\the-merismos\src\index.ts

Do not edit index.ts in THIS folder - it is a byte-faithful mirror
(SHA256 at the copy: E4AE3FFF02C0FE3E7B2315DE705DB0B8990B1719A44FFDA763D2A989F3A775A6),
taken 2026-09-02 alongside `src/lib/sphragis/`, the same day the water itself
was built.

WHY THIS REALM CONSUMES IT

The contributors — who made it, as columns, never as prose to re-argue. A
`Sphragis.collaborators` on a ware's licence is shaped exactly as this
water's `Merismos`: who, a free-text role that is RECORDED AND NEVER
WEIGHED, a yes and the moment it was given. This realm never imports the
sibling water into the sphragis mirror (neither water imports the other, by
law); it is mirrored here in full so the residual-system page can cite
`combine()`'s picture — the platform's ten, the artist's ninety, and the
ninety divided EQUALLY among the contributors — as the contributors' half of
the residual pool. Nothing here moves a cent; `shares` and `combine` return
numbers to read, and the real arithmetic stays server-side where the money
lives.

THE RULING, and it is the whole of the arithmetic. KP ⚛, verbatim: "there is
nothing to do but divide by the number of contributors, regardless of role."
And this repo's own `docs/business/financial-ecosystem.md` 140-142:
"everything left ──▶ THIS ITEM'S CONTRIBUTORS, divided EQUALLY • the main
artisan is ONE OF THEM • no ranking, no percentage shares." The 2026-09-02
first build gave each Part `points` in basis points summing to 10000; that
weighting is REMOVED, not deprecated, and a document still carrying it
parses — carried, ignored, and told. NO DIAL LIVES IN THIS WATER: the
residual dial (0–50% per product) and the covenant dial (0–50% per vessel)
are the platform's, live in that same document and in
`docs/architecture/residual-system.md`, and are applied before a number ever
reaches this division.

Read alongside: `src/lib/sphragis/` (the-sphragis), whose `collaborators`
field this water's `Merismos` assigns straight into, by shape.

Record: `.journals/realm/2026-09-02-the-seal-on-every-ware.md`, RUN-LOG.md.
