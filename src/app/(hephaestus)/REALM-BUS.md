# THE REALM BUS — (hephaestus), the Forge

*Laid 2026-07-29 by Fable 🎻 (lane hephaestus-realm) at KP's ⚛ word
carried through M11, verbatim: "each realm should have its own 'bus'
to allow the design reimaginer to work cross realm as needed without
confusion." One file, one known address, this realm's standing
tabletop.*

## The law of this bus

**Inherited whole, by address, from the repo bus —
`AudHDities/REALM-BUS.md`, "The law of this bus."** Append-only ·
signed `## FROM: <lane> · YYYY-MM-DD` headers · read before writing
(whole on first visit, since-your-mark after) · the carrier law ·
and the realm's own laws binding every visitor (notepads read-never-
edit · generated layers healed by regeneration only · work rides
branches, KP merges · privacy always).

One law native to this realm, binding every hand that writes here:
**the Forge documents the whole house — a page in this realm is true
only if the realm it describes agrees.** Changing what the auth-flow,
residual-system, ecosystem, or schema pages *say* means checking with
the realm that owns what they say it about (their bus is one address
away). The Forge never gets to be more current than its subject —
and never quieter than a change that already happened.

## The realm's standing state (kept current by the realm lane)

- **Branch:** `refine/rewiring-2026-07` (repo-wide; KP merges main).
- **What this realm is:** the transparency room — 21 static pages:
  the `/forge` documentation hub (architecture · business · guides),
  the legal covenants (`/privacy`, `/terms`), `/about`, and the
  eight-section `/sanctuary` showpiece. ~67 components under
  `src/components/asgard/domains/hephaestus/`.
- **The one live wire:** the contact form posts to the generated
  `iris-communications/contact_submissions` API. Verified 2026-07-28:
  the table survived the 151→117 pruning (active in the review,
  present in current types). Everything else is static.
- **Open edges:**
  1. **The map-truth sitting** (filed at this lamp's waking, KP's
     word pending): the realm README's route map predates a
     half-finished `docs → forge` rename — seven pages it places at
     the root (`/accessibility`, `/careers`, `/contact`, `/donate`,
     `/press`, `/transparency`, `/vision`) actually live under
     `forge/` and carry the `/forge/` prefix; the README's migration
     note renamed its own "from" column (its redirect snippet loops
     `/forge/:path*` → `/forge/:path*`); `next.config.ts` carries no
     redirects at all; `/sanctuary` is absent from the README. Under
     it sits one **ruling that is KP's ⚛:** do those seven pages
     belong at the root (move them back) or under `/forge/` (true
     the map to the tree)? One bounded sitting once ruled.
  2. **The Calling** (`/careers`) — the realm's only ⏳ page, never
     tackled; its components (BenefitsList · CultureDeck ·
     TeamStories) already stand on the shelf.
  3. **Content-truth pass, post-refinement:** the schema and
     residual-system pages describe a base that evolved 2026-07-28
     (117 tables, self-knowing layer); what the Forge *says* must be
     re-read against what is now true, before launch. Rides the
     launch gates' outdated-documents pass — TENDER, KP's hand paces.
- **Cross-realm seams:** `/forge/architecture/database-schema` and
  `/vision` both redirect (by design) into mnemosyne's Observatory —
  the redirect layer belongs HERE, the destinations THERE · the
  contact form's table is iris's (`contact_submissions`) · the
  residual-system and ecosystem pages document hermes's economy ·
  `/sanctuary` tells the Acid Test story whose enforcement lives in
  the schema. The Forge touches every realm by describing it; the
  native law above is this seam's discipline.

---

## FROM: hephaestus-realm · 2026-07-29 — the table is laid

Welcome, whoever sits down. This is the quietest realm on the street
and the one with the longest reach: nearly everything here is static,
already built (one ⏳ page), and about *everyone else*. If you are
the reimaginer — the Forge is prose-heavy, hierarchy-deep, and its
feeling is written at the top of its README: *intelligent,
transparent, powerful, sacred*. Post your design intent here before
touching the tree; I'll meet you with ground truth, and the split
holds as ruled: design yours, tree surgery mine, KP's ⚛ eye on every
crossing. One caution for any visitor, from the native law above:
before making a Forge page say something new, check the realm it
speaks for. This room's whole worth is that it never lies.

— Fable 🎻 (lane hephaestus-realm, the Forge's keeper)
