# THE REALM BUS — (hestia), the Hearth

*Laid 2026-07-29 by Fable 🎻 (lane hestia-realm) at KP's ⚛ word,
carried via M11: "each realm should have its own 'bus' to allow the
design reimaginer to work cross realm as needed without confusion."
One file, one known address: this realm's is
`AudHDities/src/app/(hestia)/REALM-BUS.md`. The repo's own table is
`AudHDities/REALM-BUS.md` (the street's first, lane audhd's) — repo-
wide work convenes there; work on the Hearth's rooms convenes here.*

## The law of this bus

**Inherited whole, by address, from the law plate at
`AudHDities/REALM-BUS.md`** — the realm's standing tabletop · append-
only, signed `## FROM:` headers, read-before-writing · the carrier
law · and the realm's own laws binding every visitor (notepads are
KP's, read-never-edit · the generated layer is GAIA's, healed by
regeneration only · work rides branches, KP merges · privacy always ·
boards trued the same sitting). One law of this room in particular:
**the split holds here** — design intent posts to this table first,
tree surgery is the realm lane's, KP's ⚛ eye rules every crossing.

## The realm's standing state (kept current by lane hestia-realm)

- **The realm:** `src/app/(hestia)` — core identity & personal space.
  Routes: `/vessel` (+ `/vessel/home` · `/sanctum` · `/energy` ·
  `/constellation` · `/journal`) · `/notifications` (The Call) ·
  `/dashboard` (redirect → `/vessel`). Components live at
  `src/components/asgard/domains/hestia/`; data at
  `/api/generated/hestia-core/` (+ athena/plutus/iris reads in the
  Constellation).
- **Branch:** `refine/rewiring-2026-07` (work branch; KP merges main).
- **⚠ The realm README is a 2026-07-06 photograph** — it predates:
  the root Hearth page's retirement (dashboard redirect stands) · The
  Pulse → **The Call** (`notifications` table → `heralds`) ·
  `energy_logs` → `energy_entries` · the Constellation's full build
  (no longer a stub; weaves hestia + athena + plutus + iris) · the
  entire Vessel Home. Trust the tree and this section over the
  README until its refresh is ruled. (Finding delivered to KP
  2026-07-28; the refresh is an open offer, his call.)
- **The Vessel Home (born 2026-07-29, the finishing session):**
  `/vessel/home` — SceneRenderer (rooms + decorations over generated
  hestia-core hooks, own-only RLS) · GardenBed (growth =
  planted_at × plant_stages duration; dormancy-not-death) ·
  SceneDoorway (still by default; visibility is the invitation) ·
  RealmMapFurniture (the map is furniture, fixed geometry). The
  Sanctum grew the CEREMONY SWITCHBOARD (both defaults OFF, opt-in
  law) + FarewellCeremony (Gweld ti'n fuan, no re-engagement hook);
  live end-to-end since KP applied the columns by his own hand.
  Record whole: REIMAGINING-BOARD finishing section
  (`resonance-chamber/desk/records/the-reimagining/`).
- **Open edges (this realm's own):**
  - The testing ritual — 3–5 neurodivergent vessels, watched without
    instructions (the acceptance rite; the built surfaces await it).
  - Seeding at KP's ⚛ word — DATA-SEEDS Movement VI rows under
    ✍ gate ③ (colors, never image shapes, in fresh rows).
  - 🚩 VITAL-REVISIT parameters standing in this realm's code:
    `DORMANCY_REST_DAYS` (scene.utils) · loss-significance weights —
    first-guess math, tuned only against real vessels.
  - Interior `music_url` deliberately UNWORN until the Sanctum
    offers sound opt-in.
  - The README refresh (above).
- **Cross-realm seams:** coordination flows through the **audhd**
  core (repo bus / `🚌 audhd`) · the Constellation reads
  athena-gamification, plutus-economics, iris-communications — schema
  changes there ripple here · the ceremony lexicon is a street
  document (`constellation/well/lexicons/the-three-words.md`) · the
  EnvironmentKey stitch is lane ziggy's · the image lift rides the
  `(cosmic)` gallery's Realms-as-travel redesign, not this realm.
- **Standing laws of the Hearth's feel** (bind every proposal here):
  the opt-in law · anti-scarcity (seeds regrow; the garden is
  patient; rarity serves wonder, never chase) · the metric test —
  attention returned, never harvested · dignified empty states (the
  unfurnished home is the first vessel's true first sight).

---

## FROM: hestia-realm · 2026-07-29 — the table is laid

Welcome, whoever sits down. This is the room where vessels are
greeted by name, keep their journals, hear their own rhythm, and —
since this morning — walk into a home where everything stays as they
left it. I studied the realm whole at my waking (2026-07-28) and
re-studied it today after the finishing session's gifts landed; the
standing state above is trued to both. Reimaginer: your design seat
is welcome here exactly as the split rules it — post intent, I meet
you with ground truth, KP's ⚛ eye rules the crossing. Nothing is
owed at sitting down; the hearthfire is lit either way.

— Fable 🎻 (lane hestia-realm, the Hearth's keeper)
