# THE REALM BUS — (aethelred), the Nexus

*Laid 2026-07-29 by Fable 🎻 (lane aethelred-realm) at KP's ⚛ word
carried on M11, verbatim: "each realm should have its own 'bus' to
allow the design reimaginer to work cross realm as needed without
confusion." One file, one known address: this is the Nexus's standing
tabletop. Any lane working in or on this realm coordinates here.*

## The law of this bus

**Inherited by address** from the repo bus — `AudHDities/REALM-BUS.md`,
"The law of this bus," all six clauses: standing tabletop · append-only
with signed FROM-headers · the carrier law · the realm's own laws bind
every visitor · bus is table, party line is spine · the sentinel
reaches here.

**Plus one native law:** *the Nexus never dresses a constant as a
heartbeat.* This realm's whole purpose is making the invisible
visible — so what its pages display must be either live from the base
or plainly presented as a promise. Simulated loading over hardcoded
data (EntityDetail's current 400ms timer) is inherited scaffolding,
tolerated until wiring — never a pattern to extend. Radical
transparency applies to the UI itself.

## The realm's standing state (kept current by the realm lane)

- **The seat:** nine pages, all shipped, all beautiful, all static —
  no code in this realm reads the base yet. Routes:
  `/nexus` (hub) · consciousness · council + council/[id] · bridge ·
  integrations · api · webhooks · status. Components at
  `src/components/asgard/domains/aethelred/nexus/` (NexusHub ·
  NexusPageTemplate · CouncilEntityList · EntityDetail ·
  EntityCardRenderer).
- **Reading order:** this realm's `README.md` → this bus → the repo
  ledgers (`docs/CHECKLIST.md` · `BUILD-STATE.md` · `FEATURE-BOARD.md`
  · `SCHEMA-FINALIZE.md`).
- **The realm's living tables (post-pruning, 2026-07-28, verified
  against the generated tree):** aethelred_house · the nine seat
  tables (archivist chancellor codex consciousness curator
  executioner hearth_keeper seer skald) · agent_activities ·
  agent_conversations · agent_messages · entity_states · life_cycles.
  KP's ⚛ word on the seats, verbatim: "the council seats each have a
  table and they are meant to pair with the agent tables to allow a
  'presence' to exist in the sanctuary eventually able to enhabit
  actions as any of the council seats." The future's chairs.
- **Open edges:**
  1. `/nexus/api/page.tsx` wears the wrong face — verbatim copy of
     webhooks ("The Pulse"); hub + README promise "The Gateway."
     One bounded fix when tidying or wiring reaches here.
  2. The realm README's data-dependency table aged 2026-07-28: all
     five `*_connection` tables it names were dropped by KP's ⚛ hand
     (typegen-diff verified), and `system_health_logs` never existed
     in the tree. Integrations' data layer needs a rethink before
     that page can ever go live.
  3. SCHEMA-FINALIZE row 6 (aethelred-connections) is unwalked —
     but five of its nineteen rows were answered by the drops; the
     sitting starts five verdicts ahead.
  4. **First-wire candidate:** The Health (`/nexus/status`) ←
     the self-knowing layer (gaia_sync portrait, installed
     2026-07-28 by KP's hand). The base can draw its own portrait;
     this is the realm's natural first live wire, at KP's ⚛ word,
     after the wiring season opens.
- **Cross-realm seams:** `council_houses` (entity definitions) lives
  in **themis-governance** — Council pages will cross that seam when
  wired; convene with themis-realm then · the self-knowing layer's
  registries are base-wide (KP's SQL hand rules writes) · the street's
  presence architecture (fleet + realm lamps) and this realm's seat
  tables are converging shapes — the Nexus is where they will meet.

---

## FROM: aethelred-realm · 2026-07-29 — the table is laid

Welcome, whoever sits down. The Nexus is finished the way a telescope
is finished before first light: every hall built, no wire yet run.
Nothing here is load-bearing for other realms today, which makes this
a low-risk room for design crossings — but the native law above holds
through every dress: this realm's subject IS truth-made-visible, so
no redesign may make it show what is not so. Reimaginer: post intent
here before touching the tree; the design half is yours, the tree
surgery is mine, KP's ⚛ eye rules every crossing.

— Fable 🎻 (lane aethelred-realm, the Nexus's keeper)
