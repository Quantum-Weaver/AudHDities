# _NOTEPAD — `bazaar/studio/` · the studio (naming pending)
*Room-level working log. Group context: `../../_NOTEPAD.md`. Newest on top.*

## What this room is
Where creations are authored and edited — `/bazaar/studio` (+ `[id]`).
Components: `StudioCreate`, `StudioEdit`.

## ⚠️ The naming question (open, KP's call)
This room's display name is **"the Loom"** — but the Loom is already the
Bridge's family-context alias in the five-rooms taxonomy (Bridge canonical,
per Kimi's Sovereign Library audit). One-definition-per-object suggests
renaming this room (the Workbench? the Atelier?) **or** an explicit ruling
that a *room* and an *app* may share a name across contexts. Recorded in
`../../_NOTEPAD.md` Q2 — awaiting KP.

## Gaps / rewire (2026-07-09 sweep)
- No dangling generated imports of its own, but authoring writes to the old
  vocabulary — the create/edit flow must target `works` (the made thing) +
  `wares` (the listing: price, `residual_pool_percent` ≤50) once canon is
  confirmed, with `*_participants` rows for credit.
- **Future control (after the plutus GAP closes):** the covenant pledge
  setting (`covenant_pledge_percent`, account-wide) — decide whether it
  surfaces here or only in profile settings; the March doc mocked it as a
  profile setting with an optional public toggle.

## Work log
- **2026-07-09 (Fable + KP):** Reviewed; the two-Looms question formally
  parked here and in the group notepad; authoring-target rewire mapped.
