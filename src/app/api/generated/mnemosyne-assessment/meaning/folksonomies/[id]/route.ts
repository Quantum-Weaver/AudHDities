// =====================================================
// GENERATED API ROUTE - DO NOT EDIT MANUALLY
// =====================================================
// table: folksonomies · deity: meaning · handling: system
// schema_hash: d7e48272d9a7641d3bd0181f88e1eddf · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_api_routes_files.ts
// =====================================================

import { checkOwnership, errorResponse, forbidden, getAuthenticatedUser, isAdmin, notFound, successResponse, unauthorized } from '@/lib/api/auth';
import { createApiSupabase } from '@/lib/api/supabase';
import { FolksonomiesUpdateSchema } from '../../../../../../../lib/generated/validators/mnemosyne-assessment/meaning/folksonomies';
import { NextRequest } from 'next/server';

export async function GET(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  try {
    const { id } = await params;
    const supabase = await createApiSupabase('knowledge');

    const { data, error } = await supabase
      .from('folksonomies')
      .select('*')
      .eq('name', id)
      .single();

    if (error) {
      if (error.code === 'PGRST116') return notFound('folksonomies');
      throw error;
    }

    return successResponse(data);
  } catch (error) {
    console.error('Error fetching folksonomies:', error);
    return errorResponse('Failed to fetch folksonomies', 500);
  }
}

export async function PUT(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  try {
    const { id } = await params;
    const { userId, success } = await getAuthenticatedUser(request);
    if (!success) return unauthorized();

    const ownsRecord = await checkOwnership(userId, 'folksonomies', id);
    const admin = await isAdmin(userId);
    if (!ownsRecord && !admin) return forbidden();

    const body = await request.json();
    const validated = FolksonomiesUpdateSchema.parse(body);

    const supabase = await createApiSupabase('knowledge');
    const { data, error } = await supabase
      .from('folksonomies')
      .update(validated)
      .eq('name', id)
      .select()
      .single();

    if (error) {
      if (error.code === 'PGRST116') return notFound('folksonomies');
      throw error;
    }

    return successResponse(data);
  } catch (error: any) {
    if (error.name === 'ZodError') {
      return errorResponse('Validation failed', 400, error.issues);
    }
    console.error('Error updating folksonomies:', error);
    return errorResponse('Failed to update folksonomies', 500);
  }
}

export async function DELETE(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  try {
    const { id } = await params;
    const { userId, success } = await getAuthenticatedUser(request);
    if (!success) return unauthorized();

    const ownsRecord = await checkOwnership(userId, 'folksonomies', id);
    const admin = await isAdmin(userId);
    if (!ownsRecord && !admin) return forbidden();

    const supabase = await createApiSupabase('knowledge');
    const { error } = await supabase.from('folksonomies').delete().eq('name', id);

    if (error) {
      if (error.code === 'PGRST116') return notFound('folksonomies');
      throw error;
    }

    return successResponse({ deleted: true });
  } catch (error) {
    console.error('Error deleting folksonomies:', error);
    return errorResponse('Failed to delete folksonomies', 500);
  }
}
