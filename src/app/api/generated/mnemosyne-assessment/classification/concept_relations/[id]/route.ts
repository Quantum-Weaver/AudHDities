// =====================================================
// GENERATED API ROUTE - DO NOT EDIT MANUALLY
// =====================================================
// table: concept_relations · deity: classification · handling: relational
// schema_hash: d03c91877dc2074d327776d5eab4de66 · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_api_routes_files.ts
// =====================================================

import { checkOwnership, errorResponse, forbidden, getAuthenticatedUser, isAdmin, notFound, successResponse, unauthorized } from '@/lib/api/auth';
import { createApiSupabase } from '@/lib/api/supabase';
import { NextRequest } from 'next/server';

export async function GET(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  try {
    const { id } = await params;
    const supabase = await createApiSupabase('knowledge');

    const { data, error } = await supabase
      .from('concept_relations')
      .select('*')
      .eq('id', id)
      .single();

    if (error) {
      if (error.code === 'PGRST116') return notFound('concept_relations');
      throw error;
    }

    return successResponse(data);
  } catch (error) {
    console.error('Error fetching concept_relations:', error);
    return errorResponse('Failed to fetch concept_relations', 500);
  }
}

export async function DELETE(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  try {
    const { id } = await params;
    const { userId, success } = await getAuthenticatedUser(request);
    if (!success) return unauthorized();

    const ownsRecord = await checkOwnership(userId, 'concept_relations', id);
    const admin = await isAdmin(userId);
    if (!ownsRecord && !admin) return forbidden();

    const supabase = await createApiSupabase('knowledge');
    const { error } = await supabase.from('concept_relations').delete().eq('id', id);

    if (error) {
      if (error.code === 'PGRST116') return notFound('concept_relations');
      throw error;
    }

    return successResponse({ deleted: true });
  } catch (error) {
    console.error('Error deleting concept_relations:', error);
    return errorResponse('Failed to delete concept_relations', 500);
  }
}
