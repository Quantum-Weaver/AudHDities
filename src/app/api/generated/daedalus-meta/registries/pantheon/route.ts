// =====================================================
// GENERATED API ROUTE - DO NOT EDIT MANUALLY
// =====================================================
// table: pantheon · deity: registries · handling: system
// schema_hash: bdf2068a983e76d181c76fb647205301 · source sha256 36d8c6da148b
// Generated by resonance-gaia src/pieces/generate_api_routes_files.ts
// =====================================================

import { errorResponse, getAuthenticatedUser, getFilters, getPaginationParams, getSortParams, successResponse, unauthorized } from '@/lib/api/auth';
import { createApiSupabase } from '@/lib/api/supabase';
import { PantheonInsertSchema } from '../../../../../../lib/generated/validators/daedalus-meta/registries/pantheon';
import { NextRequest } from 'next/server';

export async function GET(request: NextRequest) {
  try {
    const supabase = await createApiSupabase('knowledge');
    const { page, limit } = getPaginationParams(request.nextUrl);
    const filters = getFilters(request.nextUrl);
    const { column: sortColumn, ascending } = getSortParams(request.nextUrl);

    let query = supabase.from('pantheon').select('*', { count: 'exact' });

    Object.entries(filters).forEach(([key, value]) => {
      query = query.eq(key, value);
    });

    query = query.order(sortColumn, { ascending });

    const from = (page - 1) * limit;
    const to = from + limit - 1;
    query = query.range(from, to);

    const { data, error, count } = await query;
    if (error) throw error;

    return successResponse({
      data,
      pagination: { page, limit, total: count || 0 }
    });
  } catch (error) {
    console.error('Error fetching pantheon:', error);
    return errorResponse('Failed to fetch pantheon', 500);
  }
}

export async function POST(request: NextRequest) {
  try {
    const { userId, success } = await getAuthenticatedUser(request);
    if (!success) return unauthorized();

    const body = await request.json();
    const validated = PantheonInsertSchema.parse(body);

    const supabase = await createApiSupabase('knowledge');
    const { data, error } = await supabase
      .from('pantheon')
      .insert({ ...validated })
      .select()
      .single();

    if (error) throw error;
    return successResponse(data, 201);
  } catch (error: any) {
    if (error.name === 'ZodError') {
      return errorResponse('Validation failed', 400, error.issues);
    }
    console.error('Error creating pantheon:', error);
    return errorResponse('Failed to create pantheon', 500);
  }
}
