src/app/(athena)/
├── page.tsx                    # The Library (/library)
├── quests/
│   ├── page.tsx                # The Path (/library/quests)
│   └── [id]/
│       └── page.tsx            # Quest Detail (/library/quests/:id)
├── courses/
│   ├── page.tsx                # The Curriculum (/library/courses)
│   └── [id]/
│       └── page.tsx            # Course View (/library/courses/:id)
├── lessons/
│   └── [id]/
│       └── page.tsx            # The Lesson (/library/lessons/:id)
├── knowledge/
│   ├── page.tsx                # The Archive (/library/knowledge)
│   └── [id]/
│       └── page.tsx            # Scroll Detail (/library/knowledge/:id)
└── badges/
    ├── page.tsx                # The Honors (/library/badges)
    └── [id]/
        └── page.tsx            # Badge Detail (/library/badges/:id)