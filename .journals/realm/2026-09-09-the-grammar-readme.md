# 2026-09-09 — the Grammar's routes and reader named in the Mnemosyne README

`src/app/(mnemosyne)/README.md` now lists the three built Grammar routes in
its Architecture tree (`/grammar`, `/grammar/explore`,
`/grammar/atoms/[word]`) and carries a "The Grammar" section naming the
reader (`src/lib/grammar/grammar-read.ts`), the views and tables it reads,
the public/cached-by-revalidation standing of every room, the hearth/dressing
distinction, which of the four doors are wired, and the `grammar.audhdities.com`
rewrite in `next.config.ts`. The `folksonomy` row in the Database Tables
table was left untouched.
