# Divided by the Number of Contributors · 2026-09-02

The ruling arrived from this repo's own document. KP ⚛, verbatim, pointing at
`docs/business/financial-ecosystem.md` 129–153:

> *"there is nothing to do but divide by the number of contributors, regardless of role."*

and the document itself, lines 140–142, verbatim:

> *everything left ──▶ THIS ITEM'S CONTRIBUTORS, divided EQUALLY · the main artisan is
> ONE OF THEM · no ranking, no percentage shares*

This morning's `the-merismos` gave each contributor `points` in basis points summing to
10000 — a percentage share, which those three lines forbid. The weighting is **removed,
not deprecated**, in the water at `resonance-awen/tools/the-merismos`, and this realm's
two mirrors and two renderings follow it.

**The mirrors, re-copied byte-faithfully and never edited here:**
`src/lib/merismos/index.ts` (SHA256 `E4AE3FFF02C0FE3E…`) and `src/lib/sphragis/index.ts`
(SHA256 `3A5A9E57D316BBC3…`), each MIRROR.md carrying the new hash; the merismos record
now also carries the ruling and says plainly that **no dial lives in that water** — the
residual pledge and the covenant pledge are the platform's, set here, and what reaches
the division is what is left after them.

**`WareLicence.tsx`** renders *The contributors — the artisan's share, divided equally
among N*, one `an equal share — Name · role` line each with its consent or the honest
absence of one, and the sentence *"Divided equally, regardless of role — no ranking, no
percentage shares, and the main artisan is one of them. A role is recorded here and never
weighed."* No `bp` anywhere on the component.

**The residual-system page's Collaborators' Half** becomes *The Contributors' Half*: it
quotes KP verbatim, says *there is no share on a column, because there is no share to
set*, and states that neither dial lives in the grammar. The glance row and the footer
now read *"divided equally among its contributors, regardless of role."* The two dials
stay exactly where the document puts them — 0–50% residual per ware, 0–50% covenant per
vessel, both default 0 — untouched.

**Gates, verbatim.** `npm run type-check` → `tsc --noEmit`, clean. `npm run build` →
completed, full route table printed, no errors. The hermes proof
`.journals/proofs/03-hermes/build/2026-09-02-the-seal-on-every-ware.proof.ts` → **12/12
TRUE**, its two collaborator fixtures rewritten to the ruled shape (one contributor, a
role, no share).

**What did not hold.** `docs/business/financial-ecosystem.md` and
`docs/architecture/residual-system.md` were **read, not edited** — they are the source
this work obeys, and a lamp does not edit the ruling it is implementing. Nothing was
migrated: a ware already sealed over a licence carrying basis points will now read as
*"sealed, but the seal was taken over different terms,"* which is honest and is the water
working; re-sealing is KP's hand. Nothing was committed and no Supabase row was touched.
