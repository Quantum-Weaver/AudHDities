# PROOF — 01 (auth), the door

**Canvas:** https://claude.ai/code/artifact/149ac52e-58ae-4456-95ee-e21c9fb0dc86 (private)
**Drawn** 2026-08-24 · Lintel · Opus (Claude), `claude-opus-5[1m]`. Ground in
`design/` — 11 `.dc.html` artboards + `canvas.json`; refusals in `design/declined/`.

## The boards
① arrival at `/sanctuary`, as a visitor · ② `/login` · ③ `/signup` and the felt
"not now" · ④ `/forgot-password`, both states · ⑤ `/reset-password`, the recovery
visitor · ⑥ the callback's failure spoken, on `/login` · ⑦a ⑦b the same door in
the light dress. Page 2 — three declined alternatives, each with its ground.

## KP's rulings the canvas embodies — verbatim, 2026-08-24
1. **"need a felt \"not now\""** → ③
2. **"it should also fire at /sanctuary"** … **"sanctuary should be the home page
   for visitors, and the vessel should be reachable from here by logging in."** → ① ②
3. **"wait for phase 0a"** → the gradient-as-classname stays where he put it
4. **"the callback's dropped - resolve this"** → ⑥
6. **"fold into build"** → ③'s hardcodes
8. **"can we enable magic link as well as the reset password, sometimes a vessel
   has not the capacity to reset a password."** → ② ⑥

## Steps
| step | state |
|---|---|
| U — the brief, ten answers engraved | closed |
| P — ① canvas at KP's eye | closed — **"let's build this"** |
| **B — built, branch `refine/the-door-2026-08-24`** | **closed** — type-check 0 · build exit 0 · pictures in `build/` |
| G — the glance, KP's own eyes | open |
