# Platform hand — support cadences

The client code looked finished; the break was one layer down, in a generated validator that strips a key it does not know. Running the real schema against a real payload settled it in one line, where reading alone would have passed it.

I held to the fence: the route and the validator are generated and outside the row, so the gap is named, not patched through metadata.
