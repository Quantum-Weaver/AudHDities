# HANDOFF — AudHDities

***Generated strip, voiced body.** The strip is re-derived by machine every
land; everything under `## Where it stands` is someone's judgment and is
carried verbatim until a hand changes it. **Ceiling 8 KB** — past it, re-cut,
never rolled. Law: `resonance-standards/docs/THE-ROLL.md`.*

```
LEGEND   state  ● done · ◐ drifted/partial · ⏸ your hold · — not yet
         hand   ⚛ your word · ✋ your hands · 🕯️ a lamp's
         proof  ✔ gates green · ✗ failing · ? unproven
```

**AudHDities** · 2026-08-25 · last hand **Anacrusis 🎻** · branch `main` · **0 ahead / 0 behind** · working tree **2 paths** — 2 modified.
*Machine-read 11:34 by `handoff_tender.py` — **no build gate was run**; any gate figure below keeps its own date and `?`. Last commit `43a4caba1` **The Library's telling — the CHECKLIST row, the bus post, the seam-note, the step table closed; 025 as run and 026 the mend, for KP's hand; HANDOFF.md as regenerated**, Quantum Weaver 2026-08-25 11:25.*

## Where it stands

| what | state | hand | proof |
|---|---|---|---|
| The body — Next.js 16 · Tailwind 4 · Supabase · Stripe Connect | ● | 🕯️ | ? tsc 0 · build 0 · 267 pages on `main` after the merge 08-25 (`CHECKLIST.md:101`), not re-run |
| Vercel builds `main` | ● | ✋ | ? his word 08-24; `43a4caba1` live-bound (board); domain unstated (`README.md:154`) |
| The chassis — the stitch landed; Button pass · package refresh queued | ◐ | ⚛+🕯️ | ? tsc 0 · build 0 08-24 (`CHECKLIST.md:93`); board §chassis; the 3 majors ⏸ |
| (auth) the door — `/sanctuary` · `/vessel` after | ● | ⚛+🕯️ | ✔ *"door looks good"* · 7 pictures 08-24 (`CHECKLIST.md:94`) |
| (hestia) the Hearth — the frame | ● | ⚛+🕯️ | 7 pictures 08-24 (board #2) |
| (hephaestus) the Forge — economics · copy · shape halves; the record `residual-system.md` trued twice at his word 08-24 | ● | ⚛+🕯️ | ✔ V 48/52 · merged `1ad4bae83` 08-25 (`CHECKLIST.md:96-99`) |
| (hermes) the Bazaar's frame — the split · the words · works · the Loom | ● | ⚛+🕯️ | ✔ V three lenses · merged `78a001f60` 08-25 (`CHECKLIST.md:100`; `03-hermes/PROOF.md:39-48`) |
| The Bazaar's wires — every Stripe path · every signed-in walk | — | ✋ | ? unwalked — no Stripe keys, no vessel credentials (`UX-REFINEMENT-LOG.md:134`) |
| (athena) the dailies — the seventh hall, word scramble | ● | ⚛+🕯️ | `022` run at his hand · 140/140 · the four fix lines merged 08-25 (`CHECKLIST.md:95,101`) |
| (athena) the Library's frame 📚 — seven halls · the Floating Stars' three gates · the Honors earned only · the street trued · the play door | ● | ⚛+🕯️ | ✔ V three lenses · 35 pictures · merged `--no-ff` `6495bee7e` 08-25, the door `c8ebaebca` (`CHECKLIST.md:101`; `04-athena/PROOF.md:272-278`) |
| The Floating Stars' colours in the base — `025` run, `026` the mend | ⏸ | ✋ | ? `025` met 13 collections · 123 stars under longer slugs — skipped, 5 empty duplicates left; `026` unrun (`CHECKLIST.md:101`) |
| `Navigation.tsx` — the bar's Playground item swapped for Library | ◐ | ✋ | his own hand, uncommitted in the main tree 08-25 — not a lamp's (`git status`; `CHECKLIST.md:101`) |
| iris · mnemosyne · themis · aethelred · cosmic · prometheus | — | ⚛ | board §not yet; prometheus last, his word |
| `public/environments/` — 739 MB · 206 tracked files | ⏸ | ⚛ | re-measured 08-25; `.git` 1,171 MB; Pratīkṣaṇa O-b |
| User records in the base | ◐ | ✋ | anon door 08-25: 4 wares · 1 work PUBLISHED · 0 rosters; user rows walled (`CHECKLIST.md:100`) |
| `docs/CHECKLIST.md` — 56,307 B, past one-read | ◐ | ⚛+🕯️ | `wc -c` 08-25; go-live his word (THE-ROLL) |

## What waits, and whose

| what waits | whose | source |
|---|---|---|
| The glance — the Library's frame on `main` beside the Bazaar's, the Forge's shape half and the play door | ⚛ | board §What waits; `CHECKLIST.md:101` |
| The DRAFT SQL, plain — `026` (the Stars' colours) · `024` (step 2 is what the shipped build waits on; the renames HELD) · `023` (A · B · C) · then the five Stripe Prices | ✋ | `docs/sql/02[3-6]-*-DRAFT.sql`; board §What waits |
| The Bazaar's rulings — the odd cent · ④ the Loom as one form · FIX 10 and 26 · the Stripe keys · a test vessel in the live base, ever — one word closes every signed-in check · 14 lines *unwritten — his to rule* | ⚛ | `03-hermes/SPEC.md` §12; board §What waits |
| The Library's rulings — gate B's eight (the conductor's, his to strike) · *"Where this goes"* · the remembering strings · the three forms and the blend · the slug seam · the badge routes' weather · the generated types' home (GAIA's table) · the rest *unwritten — his to rule* | ⚛ | `CHECKLIST.md:101`; `04-athena/PROOF.md:79-98,220-262`; board #4 |
| The base — Sirens' ware row · Jessica's profile in the auth table, the first roster row (signups stay off) | ✋ | board §What waits |
| The Forge — weaver on `/apps/privacy` · the Terms' seven clauses · a bar item for `/` · `/enter` · the footer's six pointers · `/council/ledger`'s eye · its wash · a light theme | ⚛ | `CHECKLIST.md:98-99`; board §What waits |
| The Hearth's second pass — garden · keepsakes · companions · the hanging · the outside of the home | ⚛ | board #2 |
| The three majors, one at a time · Preview env vars · the stray canvas duplicate `193dbd09…` | ✋ | board §What waits; `CHECKLIST.md:99` |
| `public/environments/` in history — rewrite or LFS | ⚛ | Pratīkṣaṇa O-b |

## Read before you touch this repo

- **KP merges — his hand, always** (`CLAUDE.md:23`); amended 08-24: the conductor merges `--no-ff` as it goes, pushes, deletes the branch; one tree per hand (`CHECKLIST.md:96,100`).
- **A DRAFT SQL for his hand carries no decisions and no essays** — runs top to bottom, safe to re-run; the decisions are the conductor's in the CHECKLIST, his to strike (⚛ 08-25). **A reading of what the base holds reads the base**, not the seed files (`CHECKLIST.md:101`).
- **The economics, ruled 08-24:** 10% fee fixed, 30/70 of it to pool/machine; the pledge 0–50% of the 90%, default 0; distributions whole. **The split lives in one place** — `src/lib/economics/split.ts` (`CHECKLIST.md:97,100`).
- **The words:** merchant · artisan · wares — never vendor · creator · creations (⚛ 08-24). Base identifiers stay (the renames HELD in `024`); the Loom's gate `roles.includes('creator')` moves with the enum or the Loom shuts (`UX-REFINEMENT-LOG.md:134`).
- **Nothing red in the Library; never a count of the vessel** — no bar, no fraction, no points on a card (`CHECKLIST.md:101`).
- **Turbopack panics on a junctioned `node_modules` in a worktree** — copy the modules whole; the meter of record is `npm run build` on `main`; `next lint` is dead (B4).
- **`prefers-reduced-motion` blanks any room inside a framer fade-in** — ~45 files under `src/` still carry it; every `Button` renders unstyled (`CHECKLIST.md:94,99`).
- **The realm is weather, not wallpaper** — `EnvironmentLayer.tsx`; the 0.3 wash is an accessibility structure.
- **What reaches his eye is a canvas, a page, or the running app — never markdown** (⚛ 08-24); **every canvas keeps the FIT button** (⚛ 08-25; `04-athena/PROOF.md:280`).

*Voiced 2026-08-25 by the handoff-tender 🎻 (Fable, `claude-fable-5`); only this sheet was written.*

---

*No truth of its own. The ledger is `docs/CHECKLIST.md`; what waits is `resonance-chamber/desk/THE-PRATIKSANA.html`; the index is `resonance-chamber/path/handoffs/THE-HANDOFF-RACK.md`. **Where this sheet and the ground disagree, the ground is right** — regenerate it.*

— strip regenerated by `handoff_tender.py` at the hand of **Anacrusis 🎻**, 2026-08-25 11:34. **No build gate was run** — every gate figure above keeps the date it was measured. The voiced sections are carried verbatim and belong to the hands that wrote them.
