# HANDOFF — AudHDities

***Generated strip, voiced body.** The strip is re-derived by machine every
land; everything under `## Where it stands` is someone's judgment and is
carried verbatim until a hand changes it. **Ceiling 8 KB** — past it, re-cut,
never rolled. Law: `resonance-standards/docs/THE-ROLL.md`.*

```
LEGEND   state  ● done · ◐ drifted/partial · ⏸ your hold · — not yet
         hand   ⚛ your word · ✋ your hands · 🕯️ a lamp's
         proof  ✔ gates green · ✗ failing · ? unproven
```

**AudHDities** · 2026-08-25 · last hand **Anacrusis 🎻** · branch `main` · **0 ahead / 0 behind** · working tree **1 paths** — 1 modified.
*Machine-read 10:14 by `handoff_tender.py` — **no build gate was run**; any gate figure below keeps its own date and `?`. Last commit `e2a602b01` **The Bazaar's telling — the CHECKLIST row, the bus post, the seam-note, the step table closed; the seventeen pictures as read; the README's dormant prop trued; HANDOFF.md regenerated**, Quantum Weaver 2026-08-25 10:03.*

## Where it stands

| what | state | hand | proof |
|---|---|---|---|
| The body — Next.js 16 · Tailwind 4 · Supabase · Stripe Connect | ● | 🕯️ | ? tsc 0 · build 0 · 267 pages on `main` after the merge 08-25 (`CHECKLIST.md:100`), not re-run |
| Vercel builds `main` | ● | ✋ | ? his word 08-24; the board reads `4185f1c11` carried, `e2a602b01` unread; domain unstated (`README.md:154`) |
| The chassis — the stitch landed; the Button pass · the package refresh queued | ◐ | ⚛+🕯️ | ? tsc 0 · build 0 08-24 (`CHECKLIST.md:93`); board §chassis 0c · 0d; the 3 majors ⏸ |
| (auth) the door — `/sanctuary` · password + magic link · `/vessel` after | ● | ⚛+🕯️ | ✔ *"door looks good"* · 7 pictures 🗝️ 08-24 (`CHECKLIST.md:94`) |
| (hestia) the Hearth — the frame; the covenant off the vessel face | ● | ⚛+🕯️ | 7 pictures 🔥 08-24; second pass — (board #2) |
| The economics record — `docs/architecture/residual-system.md` | ● | ⚛ | trued twice at his word 08-24 (`CHECKLIST.md:96-97`) |
| (hephaestus) the Forge — economics · copy · shape halves | ● | ⚛+🕯️ | ✔ V 48/52 · merged `1ad4bae83` 08-25 (`CHECKLIST.md:99`) |
| (hermes) the Bazaar's frame — the split · the words · works beside wares · the Loom · the owner's shelf · the Exchange's ledger rows | ● | ⚛+🕯️ | ✔ V three lenses · merged `--no-ff` `78a001f60` 08-25 · U–G closed (`CHECKLIST.md:100`; `03-hermes/PROOF.md:39-48`) |
| The Bazaar's wires — every Stripe path · every signed-in walk | — | ✋ | ? unwalked: no Stripe keys in `.env`, no vessel credentials; `invoice.paid` refuses until `023` (`CHECKLIST.md:100`) |
| (athena) the dailies — the seventh hall, word scramble | ● | ⚛+🕯️ | `022` run at his hand · 140/140 published · anon read 206, 08-25 (plan §10) |
| (athena) the Library 📚 — the eye given ⚛ 08-25 · S in motion | ◐ | ⚛+🕯️ | S by an Opus hand into `04-athena/SPEC.md` — not on disk at the strip's minute; `PROOF.md:270` reads ① open; B his word |
| The Floating Stars' play door | ● | ⚛+🕯️ | merged `c8ebaebca` 08-25 |
| iris · mnemosyne · themis · aethelred · cosmic · prometheus | — | ⚛ | board §not yet; prometheus last, his word |
| The environment system — weather, not wallpaper; the affect stack named | ● | ⚛ | `bifrost/Page.tsx`; his 07-19 stroke · 08-18 design |
| `public/environments/` — 739 MB · 206 tracked files | ⏸ | ⚛ | re-measured 08-25; `.git` 1,159 MB by `du`; Pratīkṣaṇa O-b |
| User records in the base | ◐ | ✋ | anon door 08-25: 4 wares · 1 work PUBLISHED · 0 rosters; user rows walled (`CHECKLIST.md:100`) |
| `docs/CHECKLIST.md` — 50,114 B, past one-read | ◐ | ⚛+🕯️ | `wc -c` 08-25; clearing pre-live only, go-live his word (THE-ROLL) |

## What waits, and whose

| what waits | whose | source |
|---|---|---|
| The glance — the Bazaar's frame on `main` beside the Forge's shape half and the play door | ⚛ | board §What waits; `CHECKLIST.md:100` |
| The DRAFT SQL — `023-the-bazaar-refined` (step 2 `exchanges.stripe_invoice_id` is the one thing the shipped build waits on · the select policies if the signed-in read is walled · the renames · the rungs) · `023-the-ledger-door` (A · B · C) · then the five Stripe Prices | ✋ | `docs/sql/023-*-DRAFT.sql`; `docs/UX-REFINEMENT-LOG.md:134` |
| The Bazaar's rulings — the odd cent ((a) ships, four printed) · ④ the Loom as one form, the conductor's assumption · FIX 10 and 26 · a test vessel in the live base, ever · the Stripe keys · 14 lines *unwritten — his to rule* | ⚛ | `03-hermes/SPEC.md` §12; plan §10 |
| The Library — B after S · ③'s rulings (the 8 collections and 93 bubbles · the colour columns · the mythic swap …) · the three forms' rulings · seeds `024`–`026` · `02N` DRAFT · Honors earned-only | ⚛ | plan §10; board #4; `04-athena/PROOF.md` |
| The base — Sirens' ware row · Jessica's profile in the auth table, the first roster row (signups stay off) | ✋ | board §What waits |
| The Forge — weaver on `/apps/privacy` · the Terms' seven clauses · a bar item for `/` · what `/enter` is · the footer's six pointers · whose eye `/council/ledger` is for · its wash · a light theme | ⚛ | `CHECKLIST.md:98-99` |
| The Hearth's second pass — garden · keepsakes · companions · the hanging · the outside of the home | ⚛ | board #2; `CHECKLIST.md:96` |
| The three majors, one at a time · Preview env vars on Vercel · the stray canvas duplicate `193dbd09…` | ✋ | board §What waits; `CHECKLIST.md:99` |
| `public/environments/` in history — rewrite or LFS | ⚛ | Pratīkṣaṇa O-b |

*Passed on the ground, for the Pratīkṣaṇa's tender: H-b (08-01, `CHECKLIST.md:28`) · O-a (`1a31cb558` 08-24) · H-d (the stitch, 08-24).*

## Read before you touch this repo

- **KP merges — his hand, always** (`CLAUDE.md:23`); amended 08-24: the conductor merges `--no-ff` as it goes, pushes, deletes the branch; one tree per hand — worktrees (`CHECKLIST.md:96,100`).
- **The economics, ruled 08-24:** 10% fee fixed, 30/70 of it to pool/machine; the pledge 0–50% of the 90%, default 0; distributions whole; the roster all artisans. **The split lives in one place** — `src/lib/economics/split.ts` — never restated in copy (`CHECKLIST.md:97,100`).
- **The words:** merchant · artisan · wares — never vendor · creator · creations (his ⚛ 08-24). Base identifiers stay until `023` runs; the Loom's gate `roles.includes('creator')` moves with the enum rename or the Loom shuts (`UX-REFINEMENT-LOG.md:134`).
- **Turbopack panics on a junctioned `node_modules` in a worktree** — copy the modules whole; the build meter of record is `npm run build` on `main` after the merge; `next lint` is dead (B4).
- **`prefers-reduced-motion` blanks any room inside a framer fade-in** — fixed in three legal components; ~45 files under `src/` still carry it. Every `Button` renders unstyled — `bg-[${TOKEN}]` template strings (`CHECKLIST.md:94,99`).
- **The realm is weather, not wallpaper** — `EnvironmentLayer.tsx`; the 0.3 wash is an accessibility structure (08-18, in git); no engine belongs here.
- **What reaches his eye is a canvas, a page, or the running app — never a markdown file** (⚛ 08-24); **every canvas keeps the FIT button** (⚛ 08-25; plan §10).

*Voiced 2026-08-25 by the handoff-tender 🎻 (Fable, `claude-fable-5`); nothing in this repo was edited.*

---

*No truth of its own. The ledger is `docs/CHECKLIST.md`; what waits is `resonance-chamber/desk/THE-PRATIKSANA.html`; the index is `resonance-chamber/path/handoffs/THE-HANDOFF-RACK.md`. **Where this sheet and the ground disagree, the ground is right** — regenerate it.*

— strip regenerated by `handoff_tender.py` at the hand of **Anacrusis 🎻**, 2026-08-25 10:14. **No build gate was run** — every gate figure above keeps the date it was measured. The voiced sections are carried verbatim and belong to the hands that wrote them.
